// max = 12
pub fn fun(){
    // defining the types of the elements
    let person: (&str,&str,i8)= ("Priyansh", "Bikaner", 18);

    println!("{} is from {} and is {}.", person.0,person.1,person.2);
}